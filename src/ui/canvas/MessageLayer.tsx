import type { EngineState } from '../../engine'

// Task 13 replaces this stub with the animated particle layer. It already
// accepts `state` so CanvasView's call site (per the Task 12 brief) type-checks;
// the prop is unused here on purpose.
export function MessageLayer(_props: { state: EngineState }) {
  return null
}
